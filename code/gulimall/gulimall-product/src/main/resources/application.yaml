spring:
  datasource:
    username: root
    password: 12345li
    url: jdbc:mysql://192.168.1.102:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gulimall-product
#配置sql 映射文件位置 sql映射文件作用是 自定义复杂sql 业务
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto  # 自增主键
      logic-delete-value: 0
      logic-not-delete-value: 1
server:
  port: 11000
logging:
  level:
    com.atguigu.gulimall: debug