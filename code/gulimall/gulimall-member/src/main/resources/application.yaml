spring:
  datasource:
    username: root
    password: '@@fes945'
    url: jdbc:mysql://47.100.57.248:3306/gulimall_ums?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: gulimall-member
  cloud:
    nacos:
      discovery:
        server-addr: 47.100.57.248:8848
#配置sql 映射文件位置 sql映射文件作用是 自定义复杂sql 业务
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto  # 自增主键
server:
  port: 8000